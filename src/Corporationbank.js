import React, { Component } from 'react'

export default class Corporationbank extends Component {
    render(propes) {
        return (
            <div>
                <h1>Coporationbank is located at{this.props.location}</h1>
                
            </div>
        )
    }
}
